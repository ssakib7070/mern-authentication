# mern-authentication

## Installation

Copy config.env.example file and rename it with config.env. Configure the .env.example file currectly

```sh
npm install
cd client
npm install
cd ../
npm start
```

